// import styles from "./page.module.css";

import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Blog | Movie App",
  description: "Movie Database by create next app",
};

const Blog = () => {
  return <h1>Blog</h1>;
};

export default Blog;
